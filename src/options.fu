
pub let DEV_DisableBCK = 0x1 << 0;


// Zeroinit = default build options.

pub struct Lint
{
    maxwarn: i32;
};

pub struct Options
{
    using lint?: Lint;
    break_notes?: notes::SolverNotes;

    dev?: u32;
};

pub fn Lint_lax() =
    Lint(maxwarn: 10);
