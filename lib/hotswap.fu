
pragma include("<dl/hotswap.hpp>");

fn here(): void
{
    pragma output(
    `
        fu::hotswap_here();
    `);
}

fn enabled(): bool
{
    mut ret = false;

    pragma emit(
    `
        #ifdef fu_HOTSWAP
        `ret` = true;
        #endif //
    `);

    return ret;
}
